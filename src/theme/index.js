import { DefaultTheme } from "react-native-paper";
const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
     black: "#000000",
     blue: "#094293",
     primary: "#007dff",
     turquoise: "#00cce9",
     white: "#fff",
  },
};

export default theme;
